# Carpeta de datos

La base SQLite se crea por defecto en `%LOCALAPPDATA%\sistema-examen-mvc-accesible\exam.sqlite` para evitar conflictos con OneDrive.

Si el proyecto se ejecuta fuera de OneDrive o en Linux/macOS, se puede definir `DB_PATH` para guardar la base dentro de esta carpeta.
